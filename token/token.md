	THERE'RE SIX TOKEN IN C LANGUAGE	

1. Identfier
	
	what is an identifier in C ?
	
It a name used to represent a memory address

eg : memory --> 0xa34df768f (age)

	Naming convension :
	-- camel case  (ShowCase)
	-- normal      (showcase)
	-- under score (show_case)

2. Keywords

	what are keywords in C ?

This are used by gcc or computer, which perfors
differece purpose.

there are 32 keywords in C :

 auto   for  int  while  do  goto  volatile  

 sizeof  char  if  else  double  float  typedef

 struct  union  enum  void  unsigned  signed 

 break  continue long  switch  case  default 

 extern  register  static  const   short  return


	categorising the C keywords

1. Basic Data Types : 5

These keywords are used  to define the type
of data.

 --	char
 --     int 
 --	float
 --	double
 --	void

2. Types modification : 4

These keywords modify the properties of data types

 --  	short
 --	long
 -- 	signed
 --	unsigned

3. Complex types : 4

These are used to define more complex data 
structures.

 -- 	struct
 --	union
 -- 	enum
 -- 	typedef 

4. Storage classes : 4

These keywords defines the scope and life time
of variables.

 -- 	static
 --	auto 
 --	extern
 --  	register

5. Control flow : 12
These are used for controling the flow of execution

  * Conditional : 5
	--	if
	-- 	else
	--	switch
	--	case
	--	default
  

  * Loops : 5
	--	for
	--	while
	--	do
	--	break
	--	continue


  * Jumps : 2
	--	goto
	--	return


6. Types qualifier : 2

These provides additional information about the 
variable.

	-- 	const
	--	volatile

7. Operator : 1

These is an operator keyword

	-- 	sizeof


3. Constants

	what are Constants in C ?

Constants are the value you initialize to variables.

eg :	ASCII --> America standard code for
	  	  information interchange --> 256

	UNICODE --> contain all languages


4. String Literal

	what is string literals in C ?

It a way to represent string likie "hello world".

NOTE : In C language there is no format to represent
       string except using the concept of pointers.

eg :  "hello world"


5. Operator

	what are Operator in C ?

These are symbols that are use for arithmetic,
comparison, bitwise, logical.

	1. Arithemetic
		-- 	+
		--	-
		--	/
		--	*
		--	%

	2. Comparison
		--	<
		--	>
		--    	<=, >=, !=

	3. Logical Operator
		-- 	||
		--	&&
		--	^
		--	!

	4. Bitwise Operator
		-- 	&
		-- 	|

	
6. Punctuation
	
	what are punctuations in C ?

They are symbols used to perform different features
such as Terminate a variable, condition, jumps
Trigraph and format specifiers.

	Terminate variable
		--	;

	Condition
		--	?
		--	:

	Jumps
		--	:

	Trigraph
		--	using (??) plus other symbol
		--	??=	#
		--	??/	\
		--	??'	^
		--	??(	[
		--	??)	]
		--	??!	|
		--	??<	{
		--	??>	}
		--	??- 	~

	Format Specifier
		--	%f
		--	%d
		--	%ld
		--	%lf
		--	%x
		--	%p
		--	%i
		--	%
		
 
